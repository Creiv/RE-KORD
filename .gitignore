# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
release
coverage
*.tsbuildinfo
.vite

# Env / local config (keep secrets out of git)
.env
.env.*
!.env.example
!.env.docker.example

# Docker bind-mount data (local)
docker-data/

*.local

# Server runtime — percorsi libreria, cookie, account, backup config (mai in git)
server/music-root.config.json*
server/youtube-cookies.txt
server/accounts/

# Log attività (percorsi assoluti, cartelle, URL) — anche legacy in server/
**/kord-activity.log.jsonl
**/rekord-activity.log.jsonl

# One-shot rebrand migration scripts (local only, not shipped)
scripts/rename-to-rekord.mjs
scripts/fix-rekord-overreplace.mjs
scripts/fix-rekord-final.mjs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

server/bin/yt-dlp
server/bin/yt-dlp.exe
!server/bin/.gitkeep
